# traefik-sslcerts-to-files
Dump all ssl certs file generated from Traefik in acme.json (let's encrypt) to single folder/file per certs

Example:

python3 dump_all_certificate.py acme.json certsDumps/
